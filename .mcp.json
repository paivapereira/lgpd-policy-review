{
  "mcpServers": {
    "policy-reader": {
      "command": "uv",
      "args": [
        "run",
        "python",
        "-m",
        "mcp_servers.policy_reader.server"
      ]
    },
    "semgrep-runner": {
      "command": "uv",
      "args": [
        "run",
        "python",
        "-m",
        "mcp_servers.semgrep_runner.server"
      ]
    }
  }
}
