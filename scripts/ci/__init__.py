"""CI edge adapters for Passo 4 (Camada 3-MVP).

GitHub-facing glue confined here (DD-P4-3): env -> TriagerInput -> run_pipeline ->
summary. The coordinator pipeline stays GitHub-agnostic; the system is git-coupled
and platform-agnostic (DD-P4-6). Contract of record: plano Passo 4 v3 ratificado.
"""
