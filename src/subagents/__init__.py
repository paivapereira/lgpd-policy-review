"""Subagents package — Pydantic I/O contracts, system prompts, and (for the
Reporter) the in-process `emit_report` tool factory for the five pipeline
stages. Each stage is the main agent of its own `query()` (pattern A'',
`docs/specs/subagents/coordinator.md` §2).
"""
