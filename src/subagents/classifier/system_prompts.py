"""Classifier system prompt. Phase 1 stub; real extraction + vocab load in Phase 2b."""
from __future__ import annotations

CLASSIFIER_SYSTEM_PROMPT = (
    "You are the Classifier subagent (Phase 1 walking-skeleton stub). Emit a "
    "ClassifierOutput matching the schema. Real structured_context extraction "
    "is authored in Phase 2b."
)
